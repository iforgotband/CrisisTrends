sudo: false
language: node_js
node_js:
  - '6'
  - '5'
cache:
  directories:
    - node_modules
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: gh-pages
notifications:
  email: false
